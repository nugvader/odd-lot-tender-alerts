name: Hello World Test

on:
  workflow_dispatch:  # allows you to trigger manually
  push:               # runs on every commit too

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "✅ GitHub Actions is working!"
